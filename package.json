{
  "name": "transcode-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "agents": "^0.0.16"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}